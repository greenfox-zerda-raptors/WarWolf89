/**
 * Created by almasics on 2016.11.03..
 */
public class Workshop22 {
    public static void main(String... args){
        String first = "I am the first string!";
        String second = "I think I'm longer than first..";

        /*boolean comparisonResult = first.length() > second.length();*/
        // Decide if "first" string is longer than "second" string and store it in a variable
        // Print the value of the variable

        System.out.println(
                "the first line is " +
                ( first.length() > second.length() ? "longer":"shorter")

        );
    }
}
//expressions get evaluated
//statements get executed
//operator operands
//declaration
//conditional

//criteria based interviewing
// attributes candidate ... communicating, listeners.... numerate, methodical, systematic